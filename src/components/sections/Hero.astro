---
// Hero.astro
import modernHouse from '@/assets/img/project-modern-house.png';
---

<section id="inicio" class="relative min-h-screen flex items-start pt-32 md:pt-0 md:items-center">
  <!-- Background Image -->
  <div class="absolute inset-0">
    <img
      src={modernHouse.src}
      alt="Arquitectura moderna LARQ"
      class="w-full h-full object-cover brightness-90"
    />
    <!-- Enhanced gradient overlay for warmer atmosphere -->
    <div class="absolute inset-0 bg-linear-to-br from-[#E2C200]/8 via-[#E2C200]/5 to-[#E2C200]/2" />
    <!-- Additional warm overlay for sunset/golden hour effect -->
    <div class="absolute inset-0 bg-linear-to-t from-amber-900/20 via-transparent to-slate-900/5" />
  </div>

  <!-- Content -->
  <div class="container-corporate relative z-10 px-4 md:px-6">
    <div class="max-w-4xl">
      <div class="hero-element opacity-0" data-delay="0">
        <span class="inline-block text-[#E2C200] font-medium tracking-[0.3em] text-xs uppercase mb-3 md:mb-4">
          ESTUDIO DE ARQUITECTURA
        </span>
      </div>

      <h1
        class="hero-element opacity-0 text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-serif font-normal text-white leading-[1.1] mb-4 md:mb-6"
        data-delay="200"
      >
        Diseñamos espacios que <span class="text-[#E2C200] font-normal">transforman</span> la forma de vivir
      </h1>

      <p
        class="hero-element opacity-0 text-sm sm:text-base md:text-lg text-white/85 mb-8 md:mb-10 max-w-3xl leading-relaxed"
        data-delay="400"
      >
        Más de 6 años creando arquitectura contemporánea que integra funcionalidad, 
        estética y armonía con el entorno natural. Soluciones integrales para 
        proyectos residenciales, comerciales e institucionales.
      </p>

      <div
        class="hero-element opacity-0 flex flex-col sm:flex-row gap-3 md:gap-4 items-stretch sm:items-start"
        data-delay="600"
      >
        <a 
          href="/projects-page" 
          class="bg-[#E2C200] hover:bg-[#d4b600] text-[#3D3635] px-6 md:px-8 py-3 md:py-3.5 font-semibold tracking-wide transition-all duration-300 rounded-sm shadow-lg hover:shadow-xl text-center"
        >
          Ver Proyectos
        </a>
        <a
          href="/about"
          class="border-2 border-white/70 text-white px-6 md:px-8 py-3 md:py-3.5 font-medium tracking-wide transition-all duration-300 hover:bg-white/10 hover:border-white rounded-sm backdrop-blur-sm text-center"
        >
          Conocer la Firma
        </a>
      </div>
    </div>
  
  <!-- Scroll Indicator -->
  <a
    href="#nosotros"
    id="scroll-indicator"
    class="absolute bottom-8 left-1/2 -translate-x-1/2 text-white/50 hover:text-[#E2C200] transition-all duration-300 cursor-pointer opacity-0 hidden md:block"
  >
    <div class="animate-bounce-slow">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
      </svg>
    </div>
  </a>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-element.animate {
    animation: fadeInUp 0.8s ease-out forwards;
  }

  @keyframes bounce-slow {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(10px);
    }
  }

  .animate-bounce-slow {
    animation: bounce-slow 1.5s ease-in-out infinite;
  }

  /* Enhanced text shadow for better readability */
  h1 {
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.5),
                 0 4px 40px rgba(0, 0, 0, 0.3);
  }

  p {
    text-shadow: 0 1px 10px rgba(0, 0, 0, 0.6);
  }

  /* Ensure proper spacing on mobile */
  @media (max-width: 768px) {
    #inicio {
      min-height: 100vh;
      min-height: 100dvh; /* Dynamic viewport height for mobile */
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const heroElements = document.querySelectorAll('.hero-element');
    
    heroElements.forEach((element) => {
      const delay = element.getAttribute('data-delay') || '0';
      setTimeout(() => {
        element.classList.add('animate');
      }, parseInt(delay));
    });

    const scrollIndicator = document.getElementById('scroll-indicator');
    setTimeout(() => {
      if (scrollIndicator) {
        scrollIndicator.style.opacity = '1';
        scrollIndicator.style.transition = 'opacity 0.5s ease-out';
      }
    }, 1200);
  });
</script>